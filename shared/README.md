# Shared Package

Este directorio está pensado para alojar interfaces, tipos y constantes compartidos entre `backend` y `frontend`.

Ejemplos:

- `shared/types/` — tipos de dominio comunes.
- `shared/constants/` — constantes de configuración reutilizables.
